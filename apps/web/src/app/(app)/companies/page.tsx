import { PagePlaceholder } from "@/components/page-placeholder";

export default function CompaniesPage() {
  return (
    <PagePlaceholder
      title="Companies"
      description="Company profiles with tickers, projects, and filing history."
    />
  );
}
