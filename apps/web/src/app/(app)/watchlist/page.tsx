import { PagePlaceholder } from "@/components/page-placeholder";

export default function WatchlistPage() {
  return (
    <PagePlaceholder
      title="Watchlist"
      description="Track companies and projects with an activity feed of new filings and events."
    />
  );
}
