# Both packages ship native binaries and need their install scripts to run:
# sharp backs Next.js image optimization, unrs-resolver backs eslint-config-next.
allowBuilds:
  sharp: true
  unrs-resolver: true
